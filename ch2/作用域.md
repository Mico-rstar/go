### 作用域
从最内层词法域开始解析
```go
func f() {}

var g = "g"

func main() {
    f := "f"
    fmt.Println(f) // "f"; local var f shadows package-level func f
    fmt.Println(g) // "g"; package-level var
    fmt.Println(h) // compile error: undefined: h
}

```
new是一个预定义全局函数，所以可以在函数内做变量名
```go
func main()  {
    new := 1    // ok
}
```

